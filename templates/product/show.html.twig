{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.image }}</td>
            </tr>
        </tbody>
    </table>
    <div class="card mt-3">
        <div class="card-body">
            <a class="btn btn-outline-primary" href="{{ path('app_product_index', {'id': product.id}) }}">< Retour à la liste</a>
            <a class="btn btn-primary" href="{{ path('app_product_edit', {'id': product.id}) }}">Modifier</a>
        </div>
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {{ include('product/_delete_form.html.twig') }}
        </div>
    </div>
    

    
{% endblock %}
