{% extends 'base.html.twig' %}

{% block title %}Phase{% endblock %}

{% block body %}
    <h1>Phase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phase.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phase.name }}</td>
            </tr>
            <tr>
                <th>Descripton</th>
                <td>{{ phase.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="card mt-3">
        <div class="card-body">
            <a class="btn btn-outline-primary" href="{{ path('app_phase_index', {'id': phase.id}) }}">< Retour à la liste</a>
            <a class="btn btn-primary" href="{{ path('app_phase_edit', {'id': phase.id}) }}">Modifier</a>
        </div>
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {{ include('phase/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
