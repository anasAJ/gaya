{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status.name }}</td>
            </tr>
            <tr>
                <th>Desctiption</th>
                <td>{{ status.description }}</td>
            </tr>
            <tr>
                <th>phase</th>
                <td>{{ status.phase }}</td>
            </tr>
        </tbody>
    </table>

    <div class="card mt-3">
        <div class="card-body">
            <a class="btn btn-outline-primary" href="{{ path('app_status_index', {'id': status.id}) }}">< Retour à la liste</a>
            <a class="btn btn-primary" href="{{ path('app_status_edit', {'id': status.id}) }}">Modifier</a>
        </div>
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {{ include('status/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
