{% extends 'base.html.twig' %}

{% block title %}Source{% endblock %}

{% block body %}
    <h1>Source</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ source.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ source.url }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ source.description }}</td>
            </tr>
            <tr>
                <th>Added_date</th>
                <td>{{ source.addedDate }}</td>
            </tr>
            <tr>
                <th>Added_time</th>
                <td>{{ source.addedTime }}</td>
            </tr>
        </tbody>
    </table>

    <div class="card mt-3">
        <div class="card-body">
            <a class="btn btn-outline-primary" href="{{ path('app_source_index', {'id': source.id}) }}">< Retour à la liste</a>
            <a class="btn btn-primary" href="{{ path('app_source_edit', {'id': source.id}) }}">Modifier</a>
        </div>
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {{ include('source/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
